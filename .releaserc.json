{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "dotnet publish ThalesBridge -c Release -r win-x64 --self-contained true -p:Version=${nextRelease.version} -o publish && pwsh -Command \"Compress-Archive -Path publish/* -DestinationPath publish.zip -Force\""
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [{ "path": "publish.zip", "label": "publish.zip (self-contained, no .NET SDK needed)" }]
      }
    ]
  ]
}
